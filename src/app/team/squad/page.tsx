import { Placeholder } from "@/components/Placeholder";

// Maps to design: screens_squad.jsx (SquadScreen) + squadlib.jsx + playerlist.jsx
export default function SquadPage() {
  return (
    <Placeholder title="Pick / Edit Squad" owner="Youssef" designRef="design/screens_squad.jsx">
      The 15-player picker: 100M budget bar, formation, max-3-per-country rule,
      pitch view, player picker modal. Writes Squad + SquadPlayer rows.
    </Placeholder>
  );
}
