import { Placeholder } from "@/components/Placeholder";

// Maps to design: screens_leagues.jsx (LeaguesScreen)
export default function LeaguesPage() {
  return (
    <Placeholder title="Leagues" owner="Teammate C" designRef="design/screens_leagues.jsx">
      Mini-leagues: create/join by code, standings table with GW + total points
      and rank movement. Reads League / LeagueMember and aggregated user points.
    </Placeholder>
  );
}
