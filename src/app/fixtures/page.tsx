import { Placeholder } from "@/components/Placeholder";

// Maps to design: screens_more.jsx (FixturesScreen)
export default function FixturesPage() {
  return (
    <Placeholder title="Fixtures" owner="Teammate C" designRef="design/screens_more.jsx">
      Schedule by round + group standings tables. Reads Fixture (grouped by
      Gameweek) and computed group standings.
    </Placeholder>
  );
}
